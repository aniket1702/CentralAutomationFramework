package com.caf.automation.web.enums;

/**
 * Enum defining different remote modes available for test execution in the automation framework.
 * It includes SELENIUM and BROWSER_STACK modes for executing tests remotely on different environments.
 */
public enum RemoteMode {
    SELENIUM,
    BROWSER_STACK
}
